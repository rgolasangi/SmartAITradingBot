apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backend-models-pvc
  namespace: ai-trading
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard

